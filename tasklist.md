source ~/adami-kernel/adami_env/bin/activate

poetry run python src/adami_kernel/kernel.py

cd ~/adami-kernel
tail -f .adami_data/kernel.log

cd frontend
npm run dev

poetry add torch

claude --model qwen3.5:9b

find . -print | sed -e 's;[^/]*/;|____;g;s;____|; |;g' > project_tree.txt




# #######################################################################################


常用管理命令（请保存）
Bash# 查看服务状态
sudo systemctl status adami-kernel --no-pager

# 重启服务
sudo systemctl restart adami-kernel

# 停止服务
sudo systemctl stop adami-kernel

# 查看最新日志
tail -n 100 .adami_data/kernel.log

# 查看实时日志（推荐）
tail -f .adami_data/kernel.log

# 查看 Ollama 服务状态
sudo systemctl status ollama --no-pager



# ######################################################################################

下一步，请你将上面所有的建议，生成一份详细的工程修复指南，具体要求如下：

每个环节/步骤仅修改一个文件，如果是相邻的修改单位，涉及同一个文件，请放在同一个步骤修改。

如果解决问题涉及多个步骤，请分为多个步骤详细说明。

每个步骤告知我，修复问题需要的操作（新建/修改/删除），每个步骤涉及到的具体技术方案或方法，每个步骤的范例代码（核心修复部分），每个步骤修复完成后，需要进行的关键检测，修复的主要目的，修复后可以实现的功能，目的。

如果涉及到复杂问题的修复，请用1.1 1.2 的方式显示


请你根据以下项目的介绍，先阅读项目的官方文档和指南，分析当前Adami的短板和当前系统的实际情况，把项目集成进来。

先给我一份工程执行步骤计划（全文本，不要表格），在每个步骤告知我，执行需要的操作（新建/修改/删除），每个步骤涉及到的具体技术方案或方法，每个步骤修复完成后，需要进行的关键检测，修复的主要目的，执行后可以实现的功能，目的。如果涉及到复杂问题的修复，请用1.1 1.2 2.1 2.2…… 的方式显示
每个步骤请自动对齐readme文档更新和i18n多语言模块支持，默认使用英文注释和标记。

请针对刚才完成的任务，制定一个验收测试方案并执行，告知我验收情况



# #######################################################################################



# #######################################################################################























